name: SteveDaily Android

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build placeholder
        run: echo "SteveDaily build iniciado"
